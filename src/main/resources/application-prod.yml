spring:
  config:
    activate:
      on-profile: prod

  datasource:
    url: jdbc:mysql://order-app-db.cobw46ksedm2.us-east-1.rds.amazonaws.com:3306/orderapp
    username: admin
    password: OrderApp2025!
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      minimum-idle: 5
      maximum-pool-size: 10
      idle-timeout: 30000
      pool-name: HikariCP
      max-lifetime: 2000000
      connection-timeout: 30000

server:
  port: 8080  # ⬅️ ADICIONADO - Porta de produção
  tomcat:
    accesslog:
      enabled: true

app:
  frontend-url: https://main.d1zwzlsm520sqr.amplifyapp.com

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always
  health:
    redis:
      enabled: false

logging:
  level:
    root: INFO
    com.security: INFO
    org.springframework.web: INFO
    org.hibernate.SQL: DEBUG
  file:
    name: logs/security.log
  logback:
    rollingpolicy:
      max-file-size: 10MB
      max-history: 30